import 'package:flutter/material.dart';

class Favouirate extends StatelessWidget {
  const Favouirate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}